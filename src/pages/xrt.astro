---
import { getLocale } from "astro-i18n-aut";

const locale = getLocale(Astro.url) || "en";
const target = locale === "en" ? "/tokenomics/" : `/${locale}/tokenomics/`;

return Astro.redirect(target, 301);
---
